import { NgModule } from '@angular/core';
import { GraphSpComponent } from './graph-sp.component';

@NgModule({
  declarations: [GraphSpComponent],
  imports: [
  ],
  exports: [GraphSpComponent]
})
export class GraphSpModule { }
